# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Sprint 24.1 — assets do OCR (worker script, núcleo WASM, traineddata)
# copiados de node_modules por scripts/prepare-tesseract-assets.mjs
# (roda como prebuild/predev) — reproduzível a partir de dependências
# pinadas no package-lock.json, nunca versionado como binário solto.
public/tesseract

# Wrangler (local dev only — sem credenciais, mas é cache de execução)
.wrangler

# Saída de execução do Playwright (relatórios de falha) — as evidências
# intencionais ficam em evidence/screenshots/, que continua versionada.
test-results
playwright-report

# Cookie de sessão de teste E2E gerado pelo global-setup — nunca versionar.
e2e/.auth

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
